---
- name: Download Pleiades All in One DMG
  ansible.builtin.get_url:
    url: "{{ pleiades_url }}"
    dest: "{{ pleiades_download_dest }}"
    mode: "0644"
  when:
    - pleiades_enabled
    - pleiades_url | length > 0
